# ADR-003: Client-side KDF and encryption library

| Field | Value |
| --- | --- |
| Status | Approved |
| Owner | Human |
| Date | 2026-08-10 (approved), 2026-08-11 (status reconciled, library locked to hash-wasm) |
| Context | ADR-002 host-blind confidentiality; browser SPA |

## Context

ADR-002 requires deriving a data key from a user passphrase in the browser and
encrypting/decrypting client-side. This ADR fixes the primitives so behavior is
durable and reviewable. It is a security decision and must be human-approved.

## Options

- KDF: Argon2id (memory-hard, resists GPU cracking) vs PBKDF2 (available in native
  WebCrypto, weaker against dedicated hardware) vs scrypt.
- Symmetric cipher: AES-256-GCM (native WebCrypto, authenticated) vs XChaCha20-
  Poly1305 (via libsodium, nonce-misuse friendlier).
- Library: WebCrypto (built-in, no Argon2) vs libsodium.js / hash-wasm (Argon2id in
  WASM) vs argon2-browser.

## Decision (recommended)

- KDF: Argon2id via **@noble/hashes** (`argon2idAsync`; locked 2026-08-11),
  with per-user random salt stored server-side alongside ciphertext, and
  parameters tuned to ~1s on a typical device (documented, revisitable).
  Library note: the earlier candidate `hash-wasm` was rejected during Slice 2
  implementation — the published `hash-wasm@2.6.0` exposes no Argon2 export at
  all (only CRC/Keccak/MD/SHA/XXHash), so it could not satisfy this ADR as
  written. `@noble/hashes` is audited, actively maintained, pure-JS (no WASM
  binary to fetch or bundle — cleaner for the simple SPA), and provides Argon2id
  via a clean subpath (`@noble/hashes/argon2.js`). It pairs naturally with native
  WebCrypto for AES-GCM. The ADR's original "WASM" wording is superseded by this
  pure-JS choice; the memory-hardness guarantee of Argon2id is unchanged.
- Cipher: AES-256-GCM via native WebCrypto with a random 96-bit nonce per record.
- Derived key kept as a non-extractable CryptoKey and cached in-memory plus IndexedDB
  where the platform allows non-extractable persistence; passphrase re-entry only on
  a new device or after cache eviction.

## Consequences

- One JS dependency (`@noble/hashes` for Argon2id) plus native WebCrypto; small
  bundle cost (~145 kB gzipped total app bundle), no server crypto for user data.
  Parameters and library are the ADR-002 future-revisit point.
- Salt and KDF parameters are non-secret and stored with the ciphertext so any device
  can re-derive the key from the passphrase.
- Wrong passphrase yields a GCM authentication failure, giving a clean "wrong
  passphrase" signal without revealing plaintext.
- Frontend crypto unit tests (derive, encrypt, decrypt, wrong-passphrase, tamper)
  are required (brief §5).

Approved 2026-08-10 (work-state §2, "build slice 1"): Argon2id + AES-256-GCM.
Library locked to `@noble/hashes` 2026-08-11 (hash-wasm rejected: no Argon2 export
in published build). Status header reconciled from "pending" to Approved on
2026-08-11. Flagged for Lead review: the pure-JS `@noble/hashes` choice deviates
from the ADR's original "via WASM" wording but preserves the Argon2id primitive,
memory-hardness, and ~1s tuning.
